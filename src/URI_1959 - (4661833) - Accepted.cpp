#include <stdio.h>

int main(int argc, char const *argv[]) {
	int n, l;
	scanf("%d%d", &n, &l);
	unsigned int ui = n * l;
	printf("%u\n", ui);
	return 0;
}#include <stdio.h>

int main(int argc, char const *argv[]) {
	int n, l;
	scanf("%d%d", &n, &l);
	unsigned int ui = n * l;
	printf("%u\n", ui);
	return 0;
}