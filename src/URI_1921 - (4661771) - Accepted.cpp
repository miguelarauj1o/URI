#include <stdio.h>

int main(int argc, char const *argv[]) {
	long long n;
	scanf("%lld", &n);
	long long ll = (n * (n - 3)) / 2;
	printf("%lld\n", ll);
	return 0;
}#include <stdio.h>

int main(int argc, char const *argv[]) {
	long long n;
	scanf("%lld", &n);
	long long ll = (n * (n - 3)) / 2;
	printf("%lld\n", ll);
	return 0;
}